name: Build & Publish Windows EXEs

on:
  workflow_dispatch:
    inputs:
      tag_name:
        required: true
        type: string
        description: "The tag name to release"

permissions:
  contents: write

jobs:
  build-and-publish:
    name: Build & Publish (win-x64)
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: "10.0.x"

      - name: Run build.cmd
        shell: cmd
        run: .\build.cmd

      - name: Zip publish folder
        shell: pwsh
        run: Compress-Archive -Path .\publish\* -DestinationPath ${{ github.repository }}-${{ inputs.tag_name }}.zip

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ inputs.tag_name }}
          files: ${{ github.repository }}-${{ inputs.tag_name }}.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
