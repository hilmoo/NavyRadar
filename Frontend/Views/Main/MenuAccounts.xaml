<UserControl
    x:Class="Frontend.Views.Main.MenuAccounts"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:Shared.Models;assembly=Shared"
    xmlns:vm="clr-namespace:Frontend.ViewModels"
    x:Name="RootMenuUsers"
    d:DataContext="{d:DesignInstance Type=vm:MainMenuAccountsVm}"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" MinWidth="150" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <ListView
                x:Name="DataListView"
                Grid.Row="0"
                Margin="5"
                d:DataContext="{d:DesignInstance Type=vm:MainMenuAccountsVm}"
                ItemsSource="{Binding Accounts}"
                SelectedItem="{Binding CurrentAccount, Mode=TwoWay}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn
                            Width="50"
                            DisplayMemberBinding="{Binding Id}"
                            Header="Id" />
                        <GridViewColumn
                            Width="180"
                            DisplayMemberBinding="{Binding Username}"
                            Header="Username" />
                    </GridView>
                </ListView.View>
            </ListView>

            <Button
                Grid.Row="1"
                Width="150"
                Margin="0,0,20,20"
                HorizontalAlignment="Center"
                d:DataContext="{d:DesignInstance Type=vm:MainMenuAccountsVm}"
                Command="{Binding NewAccountCommand}"
                Content="New Account"
                IsEnabled="{Binding IsAdmin}" />
        </Grid>

        <GridSplitter
            Grid.Column="1"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            ResizeBehavior="PreviousAndNext"
            ResizeDirection="Columns" />

        <ContentControl
            Grid.Column="2"
            Margin="10"
            Content="{Binding ElementName=DataListView, Path=SelectedItem}">
            <ContentControl.Style>
                <Style TargetType="ContentControl">
                    <Setter Property="ContentTemplate">
                        <Setter.Value>
                            <DataTemplate DataType="{x:Type models:Account}">
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <StackPanel Margin="10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock
                                                Margin="0,0,0,20"
                                                FontSize="22"
                                                FontWeight="Bold"
                                                Text="Account Details" />
                                            <Button
                                                Grid.Column="1"
                                                Width="150"
                                                Margin="0,0,20,20"
                                                HorizontalAlignment="Center"
                                                Command="{Binding ElementName=RootMenuUsers, Path=DataContext.UpdateAccountCommand}"
                                                Content="Update Account"
                                                IsEnabled="{Binding ElementName=RootMenuUsers, Path=DataContext.IsAdmin}" />
                                        </Grid>

                                        <Border
                                            Height="200"
                                            Margin="0,0,0,20"
                                            BorderThickness="1"
                                            CornerRadius="4">
                                            <Grid>
                                                <Image Source="https://avatar.iran.liara.run/public/28" />
                                            </Grid>
                                        </Border>

                                        <Border
                                            Margin="0,0,0,15"
                                            Padding="15"
                                            BorderThickness="1"
                                            CornerRadius="4">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                </Grid.RowDefinitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="120" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>

                                                <TextBlock
                                                    Grid.Row="0"
                                                    Grid.Column="0"
                                                    Margin="0,0,0,10"
                                                    FontWeight="SemiBold"
                                                    Text="ID:" />
                                                <TextBlock
                                                    Grid.Row="0"
                                                    Grid.Column="1"
                                                    Margin="0,0,0,10"
                                                    Text="{Binding Id}" />

                                                <TextBlock
                                                    Grid.Row="1"
                                                    Grid.Column="0"
                                                    Margin="0,0,0,10"
                                                    FontWeight="SemiBold"
                                                    Text="Username:" />
                                                <TextBlock
                                                    Grid.Row="1"
                                                    Grid.Column="1"
                                                    Margin="0,0,0,10"
                                                    FontSize="16"
                                                    FontWeight="Medium"
                                                    Text="{Binding Username}" />

                                                <TextBlock
                                                    Grid.Row="2"
                                                    Grid.Column="0"
                                                    Margin="0,0,0,10"
                                                    FontWeight="SemiBold"
                                                    Text="Email:" />
                                                <TextBlock
                                                    Grid.Row="2"
                                                    Grid.Column="1"
                                                    Margin="0,0,0,10"
                                                    Text="{Binding Email}" />

                                                <TextBlock
                                                    Grid.Row="3"
                                                    Grid.Column="0"
                                                    FontWeight="SemiBold"
                                                    Text="Role:" />
                                                <TextBlock
                                                    Grid.Row="3"
                                                    Grid.Column="1"
                                                    Text="{Binding Role}" />
                                            </Grid>
                                        </Border>

                                    </StackPanel>
                                </ScrollViewer>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>

                    <Style.Triggers>
                        <Trigger Property="Content" Value="{x:Null}">
                            <Setter Property="ContentTemplate">
                                <Setter.Value>
                                    <DataTemplate>
                                        <TextBlock
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            FontSize="14"
                                            Text="Select an item from the list to view details." />
                                    </DataTemplate>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ContentControl.Style>
        </ContentControl>
    </Grid>
</UserControl>