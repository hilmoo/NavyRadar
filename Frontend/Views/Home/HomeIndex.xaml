<UserControl
    x:Class="Frontend.Views.Home.HomeIndex"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Frontend.Views.Home"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
    d:Background="White"
    d:DesignHeight="1000"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <wv2:WebView2
            x:Name="WebView"
            Grid.Column="0"
            Margin="10,10,10,10" />
        <Grid Grid.Column="1" Margin="0,10,10,3">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" MinHeight="100" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" MinHeight="100" />
                <RowDefinition Height="50" />
            </Grid.RowDefinitions>

            <local:TopSidePanel Grid.Row="0" />
            <GridSplitter
                Grid.Row="1"
                Height="5"
                HorizontalAlignment="Stretch"
                Background="Transparent"
                ResizeBehavior="PreviousAndNext"
                ResizeDirection="Rows" />
            <local:BottomSidePanel Grid.Row="2" />
            <local:ProfilePanel Grid.Row="3" Margin="0,5,0,5" />
        </Grid>
    </Grid>
</UserControl>