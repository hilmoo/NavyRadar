<UserControl x:Class="Frontend.Views.BottomSidePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:Frontend.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:ShipViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="1000" d:DesignWidth="300" d:Background="White">
    <Grid>
        <Border CornerRadius="8" BorderThickness="1"
                BorderBrush="{DynamicResource {x:Static SystemColors.WindowFrameBrushKey}}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Orientation="Vertical" Margin="12">
                    <TextBlock Text="{Binding Path=Ship.Id, FallbackValue='Not Found'}" HorizontalAlignment="Center"
                               TextWrapping="Wrap" />
                </StackPanel>
            </ScrollViewer>

        </Border>
    </Grid>
</UserControl>